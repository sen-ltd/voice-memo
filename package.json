{
  "name": "voice-memo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voice memo app with MediaRecorder, IndexedDB storage, and Web Speech API transcription.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
